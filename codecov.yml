# Validate changes before merge:
# curl -X POST --data-binary @codecov.yml https://codecov.io/validate

codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        informational: false
    patch:
      default:
        # Require every modified, coverable line in a PR to be covered by tests
        # ("All modified and coverable lines are covered by tests"). Genuinely
        # unreachable lines can be excluded inline with a `// codecov:ignore`
        # comment rather than lowering this gate.
        target: 100%
        threshold: 0%
        informational: false

github_checks:
  annotations: true
