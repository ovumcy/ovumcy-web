none

Test-only: a browser spec for the dashboard's calm cycle-start suggestion state
(`[data-cycle-start-suggestion]` inside `#dashboard-cycle-start`), which no e2e
lane reached before. No product code, template or copy changes.
