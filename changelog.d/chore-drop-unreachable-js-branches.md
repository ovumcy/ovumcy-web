none

Dead-code removal with no user-visible effect: three DOM branches whose targets
no template renders — a binary-toggle text state, the dashboard journal preview
block and its seven sub-targets, and a clear-data status fallback the single
form never reaches — plus the symptom-label collector only the preview used and
three e2e decoration helpers with no caller.
