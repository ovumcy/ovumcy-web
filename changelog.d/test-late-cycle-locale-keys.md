none

Tests only: a browser spec drives the three late-cycle dashboard notice states
through seeded data and asserts the rendered copy — including the plural variant
the count selects — against the shipped locale catalogues. No product code and no
locale value changes.
