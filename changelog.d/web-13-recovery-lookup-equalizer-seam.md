none

Internal: the recovery-lookup timing equalizer now spends its two bcrypt
comparisons through the same test seam as the login, registration and
settings re-auth equalizers. Its guard records each (hash, operand) pair the
shipped body hands to bcrypt at runtime instead of reading the call's source
text, the recovery work ledger measures the unknown-address baseline instead
of reading it off the placeholder constants, and the equalizer sweep now
covers func-declared equalizers too. Behaviour and timing are unchanged.
